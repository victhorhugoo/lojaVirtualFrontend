"use server";

import { CartItem } from "@/types/cart-item";

export const finishCart = async (
  token: string,
  addressId: number,
  cart: CartItem[]
) => {
  //t
  return "https://www.google.com";
};
